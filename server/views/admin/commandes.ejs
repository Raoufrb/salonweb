
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>✂️ L'Oasis Commandes</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600&family=Montserrat:wght@300;400;500;600&display=swap">
  <style>
  :root {
      --primary-color: #9c8378;
      --secondary-color: #eae0d5;
      --accent-color: #c19a6b;
      --text-color: #4a4a4a;
      --light-bg: #f9f7f4;
      --header-font: 'Playfair Display', Georgia, serif;
      --body-font: 'Montserrat', 'Segoe UI', sans-serif;
    }
    
    body {
      font-family: var(--body-font);
      background-color: var(--light-bg);
      color: var(--text-color);
      margin: 0;
      padding: 0;
      line-height: 1.6;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }
    
    .salon-header {
      background-color: white;
      box-shadow: 0 2px 15px rgba(0,0,0,0.08);
      padding: 15px 0;
      position: relative;
    }
    
    .salon-header .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .logo {
      font-family: var(--header-font);
      font-size: 28px;
      color: var(--primary-color);
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .logo i {
      color: var(--accent-color);
    }
    
    .nav-links {
      display: flex;
      gap: 20px;
    }
    
    .nav-link {
      color: var(--text-color);
      text-decoration: none;
      font-weight: 500;
      padding: 5px 10px;
      border-radius: 4px;
      transition: all 0.3s ease;
    }
    
    .nav-link:hover {
      background-color: var(--secondary-color);
      color: var(--primary-color);
    }
    
    .nav-link.active {
      background-color: var(--primary-color);
      color: white;
    }
    
    
    .page-title {
      font-family: var(--header-font);
      text-align: center;
      color: var(--primary-color);
      margin: 30px 0;
      font-size: 32px;
      font-weight: normal;
      position: relative;
    }
    
    .page-title::after {
      content: "";
      display: block;
      width: 80px;
      height: 3px;
      background-color: var(--accent-color);
      margin: 15px auto;
    }
    
    .dashboard-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }
    
    .stat-card {
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.06);
      padding: 20px;
      text-align: center;
      transition: transform 0.3s ease;
    }
    
    .stat-card:hover {
      transform: translateY(-5px);
    }
    
    .stat-icon {
      font-size: 36px;
      color: var(--accent-color);
      margin-bottom: 10px;
    }
    
    .stat-value {
      font-size: 24px;
      font-weight: bold;
      color: var(--primary-color);
      margin: 5px 0;
    }
    
    .stat-label {
      color: var(--text-color);
      font-size: 14px;
    }
    
    .content-card {
      background-color: white;
      border-radius: 12px;
      box-shadow: 0 2px 20px rgba(0,0,0,0.05);
      padding: 25px;
      margin-bottom: 30px;
    }
    
    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
    }
    
    .card-title {
      font-family: var(--header-font);
      color: var(--primary-color);
      margin: 0;
      font-size: 22px;
    }
    
    .btn {
      padding: 10px 16px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 500;
      font-size: 14px;
      transition: all 0.3s ease;
      display: inline-block;
      border: none;
      cursor: pointer;
    }
    
    .btn-primary {
      background-color: var(--primary-color);
      color: white;
    }
    
    .btn-primary:hover {
      background-color: #876d62;
    }
    
    .btn-success {
      background-color: #68b984;
      color: white;
    }
    
    .btn-success:hover {
      background-color: #57a873;
    }
    
    .btn-danger {
      background-color: #e76f51;
      color: white;
    }
    
    .btn-danger:hover {
      background-color: #d65f41;
    }
    
    .orders-table {
      width: 100%;
      border-collapse: collapse;
    }
    
    .orders-table th, 
    .orders-table td {
      padding: 16px;
      text-align: left;
      border-bottom: 1px solid #f0ebe6;
    }
    
    .orders-table th {
      font-weight: 600;
      color: var(--primary-color);
      background-color: var(--secondary-color);
      opacity: 0.8;
    }
    
    .orders-table tr:last-child td {
      border-bottom: none;
    }
    
    .orders-table tr:hover {
      background-color: var(--light-bg);
    }
    
    .status {
      padding: 6px 12px;
      border-radius: 20px;
      font-weight: 500;
      display: inline-block;
      text-transform: capitalize;
      font-size: 13px;
    }
    
    .status.en {
      background-color: #fff3cd;
      color: #856404;
    }
    
    .status.acceptée {
      background-color: #d4edda;
      color: #155724;
    }
    
    .status.refusée {
      background-color: #f8d7da;
      color: #721c24;
    }
    
    .product-item {
      margin-bottom: 5px;
      display: flex;
      align-items: center;
      gap: 5px;
    }
    
    .product-item i {
      color: var(--accent-color);
      font-size: 12px;
    }
    
    .action-buttons {
      display: flex;
      gap: 8px;
    }
    
    .empty-state {
      text-align: center;
      padding: 40px 0;
      color: #888;
    }
    
    .empty-state i {
      font-size: 48px;
      margin-bottom: 15px;
      color: var(--secondary-color);
    }
    
    .footer {
      text-align: center;
      color: #888;
      margin-top: 40px;
      padding: 20px 0;
      font-size: 14px;
      border-top: 1px solid var(--secondary-color);
    }
    
    @media (max-width: 768px) {
      .orders-table {
        display: block;
        overflow-x: auto;
      }
      
      .dashboard-stats {
        grid-template-columns: 1fr;
      }
      
      .nav-links {
        display: none;
      }
    }
  </style>
</head>
<body>
  <header class="salon-header">
    <div class="container">
      <div class="logo">
        <i class="fas fa-cut"></i>
        <span>L’Oasis – Votre Instant Beauté</span>
      </div>
      <nav class="nav-links">
        <a href="/admin/dashboard" class="nav-link ">Tableau de Bord</a>
        <a href="/admin/commandes" class="nav-link active">Commandes</a>
        <a href="/admin/rdvs" class="nav-link">RDV</a>
        <a href="/admin/services" class="nav-link">Services</a>
        <a href="/admin/produits" class="nav-link">Produits</a>
        <a href="/admin/employes" class="nav-link">Employés</a> 
      </nav>
    </div>
  </header>

  <div class="container">
    <h1 class="page-title">Gestion des Commandes</h1>
  
    <div class="dashboard-stats">
      <div class="stat-card">
        <div class="stat-icon">
          <i class="fas fa-calendar-check"></i>
        </div>
        <div class="stat-value">
          <%= typeof totalCommandes !== 'undefined' ? totalCommandes : 0 %>
        </div>
        <div class="stat-label">Total Commandes</div>
      </div>
    
      <div class="stat-card">
        <div class="stat-icon">
          <i class="fas fa-clock"></i>
        </div>
        <div class="stat-value">
          <%= typeof commandesEnAttente !== 'undefined' ? commandesEnAttente : 0 %>
        </div>
        <div class="stat-label">En attente</div>
      </div>
    
      <div class="stat-card">
        <div class="stat-icon">
          <i class="fas fa-check-circle"></i>
        </div>
        <div class="stat-value">
          <%= typeof commandesAccepteesMois !== 'undefined' ? commandesAccepteesMois : 0 %>
        </div>
        <div class="stat-label">Acceptées ce mois</div>
      </div>
    
      <div class="stat-card">
        <div class="stat-icon">
          <i class="fas fa-coins"></i>
        </div>
        <div class="stat-value">
          <%= typeof revenusCommandesMois !== 'undefined' 
                ? revenusCommandesMois.toLocaleString('fr-FR') + ' DA' 
                : '0 DA' %>
        </div>
        <div class="stat-label">Revenus du mois</div>
      </div>
    </div>

    
    <div class="content-card">
      <div class="card-header">
        <h2 class="card-title">Liste des Commandes</h2>
      </div>
      
      <% if (commandes && commandes.length === 0) { %>
        <div class="empty-state">
          <i class="fas fa-box-open"></i>
          <p>Aucune commande trouvée.</p>
        </div>
      <% } else { %>
        <table class="orders-table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Client</th>
              <th>Produits/Services</th>
              <th>Total</th>
              <th>Statut</th>
              <th>Date</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% commandes.forEach(c => { %>
              <tr>
                <td>#<%= c.id %></td>
                <td><%= c.client_nom %></td>
                <td>
                  <% c.produits.forEach(produit => { %>
                    <div class="product-item">
                      <i class="fas fa-spa"></i>
                      <%= produit %>
                    </div>
                  <% }) %>
                </td>
                <td><%= c.total %> DA</td>
                <td><span class="status <%= c.status.toLowerCase() %>"><%= c.status %></span></td>
                <td><%= new Date(c.date).toLocaleDateString('fr-FR') %></td>
                <td class="action-buttons">
                  <form action="/admin/commandes/<%= c.id %>/valider" method="POST">
                    <button type="submit" class="btn btn-success"><i class="fas fa-check"></i></button>
                  </form>
                  <form action="/admin/commandes/<%= c.id %>/refuser" method="POST">
                    <button type="submit" class="btn btn-danger"><i class="fas fa-times"></i></button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </div>
  
  <footer class="footer">
    <div class="container">
      <p>&copy; 2025 Beauté Élégance - Tous droits réservés</p>
    </div>
  </footer>
</body>
</html>